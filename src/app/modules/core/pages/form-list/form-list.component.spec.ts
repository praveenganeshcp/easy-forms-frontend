import { ComponentFixture, TestBed } from '@angular/core/testing';

import { FormListComponent } from './form-list.component';

describe('FormListComponent', () => {
  
    it('should display list of form cards', () => {})

    it('should display no forms created message if no forms was created by the user', () => {})

    it('should display error message if exception is thrown while loading the forms', () => {})

    it('should provide a option to create a new form', () => {})
});
