import { ComponentFixture, TestBed } from '@angular/core/testing';

import { CreateNewFormComponent } from './create-new-form.component';

describe('CreateNewFormComponent', () => {
  it('should display create new form title', () => {})

  it('should display input field to enter the new form name', () => {})

  it('should display error message if form name is exceeeding 25 chars', () => {})

  it('should display error message if the form name already exists', () => {})

  it('should display error message if exception is thrown while creating the form', () => {})
});
