import { ComponentFixture, TestBed } from '@angular/core/testing';

import { CoreAppShellComponent } from './core-app-shell.component';

describe('CoreAppShellComponent', () => {
  it('should display application header that is consistent across all the routes', () => {})

  it('should provide the placeholder to create app components', () => {})
});
